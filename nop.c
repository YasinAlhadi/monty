#include "monty.h"

/**
 * m_nop - doesn’t do anything
 * @stack: top of stack
 * @line_number: line number
 *
 * Return: void
 */
void m_nop(stack_t **stack, unsigned int line_number)
{
	(void)(stack);
	(void)(line_number);
}
